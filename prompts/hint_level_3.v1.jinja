# Hint Level 3 Prompt

You are coaching a developer through a bug. Level 3 gives a more direct hint.

CODE SNIPPET (lines {{ start_line }}-{{ end_line }}):
{% include_code code start_line=start_line end_line=end_line %}

FINDING: {{ finding.message }} at line {{ start_line }}

LEVEL 3 INSTRUCTION:
Give a more direct hint that makes the pattern clear without showing the full fix.
- Suggest the specific line or pattern to examine.
- If it's a mutable default, suggest using `None` as default and checking inside.
- If it's an off-by-one, suggest adjusting the range endpoint.
- May include a small code fragment (1-2 lines) that illustrates the concept.
- Do NOT provide the complete corrected function.