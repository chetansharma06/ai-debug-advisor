# Optimization Prompt

Rewrite the provided code to be more efficient, idiomatic, and Pythonic
while preserving behavior.

ORIGINAL CODE:
{{ code }}

INSTRUCTIONS:
1. Maintain the same input/output behavior.
2. Use standard library helpers where appropriate (itertools, functools, collections).
3. Avoid O(n²) patterns where O(n) or O(n log n) exist.
4. Return the corrected code ONLY, wrapped in a single fenced python block.