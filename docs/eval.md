# Evaluation

Run the evaluation harness:

```bash
python eval/run_eval.py
```

Metrics:
- Finding precision/recall against labeled dataset
- Hint quality (leak detection rate)
- Optimization success rate